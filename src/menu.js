// initial set up
// one funciton that creates a menu card and appends it
// make this link and work
// come back and refactor so multiple entries are displayed

export default function menuCard() {
  const content = document.getElementById(content);

  const textContentParagraph = document.createElement("p");
  textContentParagraph = "Flies on web noodles";

  content.appendChild(textContentParagraph);
}
